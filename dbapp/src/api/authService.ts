import { UserType } from '../context/UserContext';

export const signIn = (props: UserType) => {
  // TODO: needs to be replaced with BE authentication - fetching new auth active tocken
};

export const signOut = () => {
  // TODO: needs to be replaced with BE authentication - removing auth active token
};
